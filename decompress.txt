FUNTION: decompress
INPUT:BinaryTree t
OUTPUT: void
USAGE:descompress(BinaryTree t, " ", array<pair<string, char>>)
BEGIN
	IF isLeave:
		array.push_back(<str,t.ch>)
	ELSE:
		descompress(t->left, str+"0" array<pair<string, char>)
		descompress(t->right, str+"1" array<pair<string, char>)
	END	
END